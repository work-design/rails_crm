<%= render 'top_bar' %>

<div class="fixed-grid has-auto-count px-3">
  <div class="grid">
    <div class="box cell">
      <div class="has-colomn-centered">
        <strong><%= @contact.orders.count %></strong>
        <p>Orders Count</p>
      </div>
    </div>
    <div class="box cell">
      <div class="has-column-centered">
        <strong><%= @contact.pending_members.count %></strong>
        <p>Contacts Count</p>
      </div>
    </div>
  </div>
</div>

<table class="table is-fullwidth is-hoverable as-show">
  <tbody>
    <%= render 'show_table' %>
  </tbody>
</table>
