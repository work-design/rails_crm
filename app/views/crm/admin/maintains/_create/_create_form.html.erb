<%= form_with model: @maintain, url: { action: 'update', id: @maintain.id }, id: 'client_form' do |f| %>
  <%= render 'error_messages', target: f.object if f.object.errors.present? %>
  <%= render partial: 'form', locals: { f: f } %>
<% end %>
