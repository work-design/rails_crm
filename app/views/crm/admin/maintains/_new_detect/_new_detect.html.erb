<%= form_with url: { action: 'create_detect' } do |f| %>
  <%= f.text_field :identity, value: params[:identity], required: true, autofocus: true, autocomplete: 'off', label: Crm::Maintain.human_attribute_name('client.identity') %>
  <%= f.submit '下一步' %>
<% end %>
