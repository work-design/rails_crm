<div class="weui-form__control-area">
  <div class="weui-cells__group weui-cells__group_form">
    <div class="weui-cells__title">线索信息</div>
    <%# f.collection_select :maintain_source_id, @maintain_sources, :id, :name, all: { normal: nil } %>
    <%# f.collection_select :pipeline_id, @task_templates, :id, :name, include_blank: true, all: { normal: nil } %>
    <div class="weui-cells">
      <%= f.number_field :deposit_ratio %>
      <%= f.collection_select :payment_strategy_id, @payment_strategies, :id, :name %>
      <%= f.text_field :remark %>
      <%= f.text_area :note %>
    </div>
  </div>
</div>
