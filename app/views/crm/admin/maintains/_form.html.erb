<div class="field is-horizontal">
  <%= f.collection_select :maintain_source_id, @maintain_sources, :id, :name, all: { select: nil } %>
  <%= link_to({ controller: 'maintain_sources' }, aria: { label: '来源设置' }, target: '_blank') do %>
    <i class="fa-solid fa-cog"></i>
  <% end %>
</div>
<% if defined? RailsBench %>
<div class="field is-horizontal">
  <%= f.collection_select :pipeline_id, @task_templates, :id, :name, include_blank: true, all: { select: nil } %>
  <%= link_to({ contorller: 'bench/admin/task_templates' }, aria: { label: '流水线' }, target: '_blank') do %>
    <i class="fa-solid fa-cog"></i>
  <% end %>
</div>
<% end %>
<%= f.number_field :deposit_ratio %>
<%= f.collection_select :payment_strategy_id, @payment_strategies, :id, :name if @payment_strategies.present? %>
<%= f.text_area :remark %>
