<div class="buttons are-small">
  <%= button_to({ action: 'new_detect' }, class: 'button is-link mb-1') do %>
    <i class="fa-solid fa-plus"></i>
    <span class="pl-1"><%= t('.new_detect.title') %></span>
  <% end %>
  <%= button_to({ action: 'new_batch' }, class: 'button is-primary mb-1') do %>
    <i class="fa-solid fa-user-plus"></i>
    <span class="pl-1"><%= t('.new_batch.title') %></span>
  <% end %>
</div>
