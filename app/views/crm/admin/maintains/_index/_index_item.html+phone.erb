<p><%= model.client.identity %></p>
<p>
  <strong><%= model.client.real_name %></strong>
  <span class="color-grey pl-1"><%= model.client.nick_name %></span>
  <span class="tag">
    <i class="fa-solid <%= model.client.male? ? 'fa-male color-info' : 'fa-female color-danger' %>"></i>
    <span class="pl-1"><%= model.client.gender_i18n %></span>
  </span>
</p>
