<td>
  <div class="is-several">
    <%= image_tag model.logo, class: 'image is-24x24' if model.logo.attached? %>
    <span><%= model.name %></span>
  </div>
</td>
<td colspan="3"></td>
<td>
  <%= button_to({ action: 'create' }, params: { 'wallet[wallet_template_id]': model.id, 'wallet[type]': 'Trade::CustomWallet' }, class: 'button is-primary') do %>
    <span>开通</span>
  <% end %>
</td>
