<td>
  <div class="is-several">
    <%= image_tag model.logo, class: 'image is-24x24' if model.logo.attached? %>
    <span><%= model.name %></span>
  </div>
</td>
<td>
  <%= render partial: 'advance', collection: model.unopened_advances.without_card, as: :advance, locals: { model: model } %>
</td>
