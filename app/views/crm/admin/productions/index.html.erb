<%= render 'maintain_bar' %>

<%= render layout: 'index_raw_table' do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @productions, as: :model, cached: -> model { [ model, @cart ] } %>
<% end %>

<%= paginate @productions %>

<%= render partial: 'cart_nav', locals: { cart: @cart } %>
