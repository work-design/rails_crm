<%= render 'maintain_bar' %>

<%= render 'filter_form' %>

<%= render layout: 'index_raw_table' do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @productions, as: :model %>
<% end %>

<%= paginate @productions %>

<%= render partial: 'cart_nav', locals: { cart: @cart } %>
