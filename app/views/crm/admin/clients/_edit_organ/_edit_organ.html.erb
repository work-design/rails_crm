<%= form_with model: @client, url: { action: 'update' }, id: 'organ_form' do |f| %>
  <%= render 'error_messages', target: f.object if f.object.errors.present? %>
<% end %>

<%= button_to({ action: 'init_organ', id: @client.id }) do %>
  <i class="fa-solid fa-plus"></i>
<% end %>
