<%= link_to @maintain_tag.name, { controller: 'maintains', 'maintain_logs.maintain_tag_id': @maintain_tag.id }, class: active_helper('maintain_logs.maintain_tag_id': @maintain_tag.id, active: 'button is-info', item: 'button is-light') %>
