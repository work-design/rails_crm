<%= render 'maintain_bar' %>

<%= render layout: 'index_table' do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @cards, as: :model %>
  <%= render partial: 'template_tbody', layout: 'template_tr', collection: @card_templates, as: :model %>
<% end %>
