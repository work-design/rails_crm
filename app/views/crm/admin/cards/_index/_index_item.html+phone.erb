<div class="is-several">
  <%= image_tag model.card_template.logo, class: 'image is-24x24' if model.card_template.logo.attached? %>
  <span><%= model.card_template.name %></span>
</div>
<div class="color-grey"><%= model.organ.name %></div>
